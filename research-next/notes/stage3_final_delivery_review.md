# Independent final-delivery review

Reviewed 16 September 2026 while Qwen-7B main execution and the separate selected-case validation were unfinished. This is a source/provenance review, not a final numeric or PDF visual sign-off. The reviewer did not modify frozen experiment files, report generators, package generators or the manuscript template.

## Confirmed safeguards

- The template distinguishes a completed bounded prototype/evaluation from a completed thesis, general fault resilience, production readiness, and an A/A*-ready submission. It states the persistent numerically unequal old-prefix fault contract, every-corrupting-layer evidence requirement, trusted phases, irreversible history fallback, and output withholding.
- Native/bare are explicitly unprotected cost references. The measurement boundaries distinguish window latency, constructor-inclusive sessions, excluded common setup, injection-subtracted diagnostics, and local availability proxies rather than network streaming.
- Main tables retain separate model and initial-prefix/window shapes. Eight fault strata remain visible. Three clean repetitions do not fully counterbalance seven methods; the template acknowledges that limitation and the coupled layer/K/V/severity/time factors.
- The composer binds table generation, independent raw recomputation, corrected summaries, raw streams and audits. Its GPU/runtime checks and the existing 286-pass GPU logs agree with the stated environment. The source-set check remains a separate completion gate.
- The updated packager binds the PDF to the manuscript through the renderer's source/PDF hashes and refuses delivery until `visual_review` is `passed_all_pages`. It includes the corresponding QA directory. This closes the earlier gap in which ZIP integrity alone did not establish PDF/source identity.
- Packaging selects required earlier-stage code/tests, model-download helpers and portable PDF layout without selecting model caches or environments. It refuses symlinks and existing final output paths. The ZIP's own manifest is included in archived-byte verification. Credential-pattern scanning is appropriately described as non-exhaustive.
- The selected-case diagnostic has its own config, output directory, audits and verifier. The verifier binds the original main case, applied fault vectors, native reference, token trace, source map and replay starts. The composer now re-executes that verifier rather than accepting a status string alone.

## Required final binding check sent to root

At the reviewed revision, `manuscript_provenance.json` records the amendment hash, but the package preflight checks the selected diagnostic hash without also comparing `selected_diagnostic_amendment_sha256` to the current amendment file. Add that comparison before delivery.

Further, a hash collected only during final composition does not establish that the amendment was fixed before the selected replay. Preserve a pre-submission record of amendment, diagnostic config, verifier and batch-script hashes. Have the diagnostic job verify/print those hashes before invoking the frozen runner. Bind that declaration record and amendment into the selected-case validation output and package. This establishes the intended local execution chronology, not an externally notarized preregistration.

These are post-measurement reporting/diagnostic provenance actions. They do not require changing the frozen main-study source, data or protocol, and must not cause the selected replay to enter the main experiment's totals.

## Selected replay wording

The new template block correctly identifies the Smol code/prefix-128/W8, seed-7431 multi-prefix case as selected after observing natural fallback. Its three policy archives are separate from the 21 predeclared main archives. Its expected 13 sessions, 33 commits and three checkpoint refusals must remain separate from main correctness counts and all performance distributions. Successful reproduction of the original trace would close that case's saved-state gap; it would not create an independent fault observation or new novelty evidence.

The final sentence about no post-observation selection of a *headline* sample is compatible with this only if the selected validation remains clearly labeled and never replaces or augments main headline denominators.

## Optional presentation improvement

The current memory table shows the largest absolute GPU allocation across methods/scenarios plus journal payloads. That is useful for capacity planning but does not show protection's incremental cost relative to ordinary inference. Consider adding a compact clean-window table or sentence with bare/full/sparse/all-cuts peak allocation increments and checkpoint payload by model/shape. The independently recomputed resource groups already contain those quantities. For example, the completed Smol long-context run has roughly 137.6 MiB median sparse peak increment versus 5.9 MiB bare, whereas the sparse journal itself is only about 0.053 MiB. Reporting only the tiny journal can obscure the actual copy/staging cost.

No inference about the unfinished Qwen-7B result is made here. All final numeric claims, the selected-case completion claim, test counts after any reporting changes, and PDF page quality still require their final completion checks.
