# Post-hoc boundary-extension amendment

Declared on 16 September 2026, after inspection of the completed frozen primary and before any amended GPU measurement. Authorized by the coordinating agent; no GPU action is performed by the implementation reviewer.

The strict actual-fallback selector found no primary case with a nonzero sparse cut and a first changed decision before the end of its window. Its honest skipped record is preserved at `runs/boundary-selection-v1/metadata.json`. Both observed primary token divergences occurred at j=W=12: Qwen `primary-052` has sparse cut 0, and Qwen `primary-063` has sparse cut 6 at fault layer 11. Thus the primary alone does not execute a full-layer transition after a nonzero-cut divergence within its measured recovery window.

The sole amendment reuses `primary-063` on pinned Qwen2.5-0.5B, including its same 64-token prefix, initial token, seed 22063, layer-11 V-vector coordinates and exact recorded before/delta/after values. Only the diagnostic window changes from W=12 to W=14. `experiments/boundary_extension_amendment.json` binds this choice to the immutable primary raw digest. Frozen primary/config files are not modified, and no alternative prompts, seeds, coordinates or faults are searched.

Before accepting a result, compare the first 12 clean/faulty decisions and the complete injected-fault record against the primary, require first divergence at transition 12, and require sparse start-layer trace `[6] * 12 + [0] * 2`. All-layer starts must be `[11] * 12 + [0] * 2`; full replay starts at layer 0 throughout. Their decoder-layer work counts are respectively 264, 204 and 336 for N=24, W=14. These counts exclude heads, restoration and diagnostic continuation.

Run the unchanged primary runner with one recorded repetition and one normal-overhead repetition solely to collect diagnostics; its built-in warmups remain. No captured timing enters performance analysis. Complete saved CPU states for checkpoint, reference, no recovery, slot-only, full, sparse and all-layer recovery must fit a 20 MiB serialized budget, or capture is explicitly skipped. An independently implemented existing snapshot auditor must compare saved tensors, tokens, logits, layouts and replay traces after validating archive digests. The existing two-step live continuation checks remain, but continuation tensors are not in the archive and must not be described as independently re-executed.

This targeted example is selected after seeing primary outcomes. It is not another independent fault sample, a new fault distribution, a performance result, a general guarantee, or a replacement for the frozen primary. A failure is retained and reported, not followed by a search for another passing case. No result is asserted in this amendment.
